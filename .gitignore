*.zip git add .gitignore git commit -m Add ZIPs to .gitignore to prevent future tracking git push
*.zip git add .gitignore git commit -m Add ZIPs to .gitignore to prevent future tracking git push
*.zip
